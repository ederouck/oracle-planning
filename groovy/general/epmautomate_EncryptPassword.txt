/* RTPS: {User},{Password} */
 
EpmAutomate automate = getEpmAutomate()
 
//Encrypt the password for the given user in the source environment
def passwordFile = rtps.User.toString() + '.epw'
def myPassword = rtps.Password.toString()

EpmAutomateStatus encryptstatus1 = automate.execute('encrypt', myPassword,'KatoenNatie',passwordFile)

if(encryptstatus1.getStatus() != 0) throwVetoException(encryptstatus1.getOutput())
println(encryptstatus1.getOutput())
